apiVersion: v1
kind: Secret
metadata:
  name: {{ include "my-new-chart.fullname" . }}-quotes-secrets
  labels:
  {{- include "my-new-chart.labels" . | nindent 4 }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
data:
  homepageDisplay: {{ required "quotesSecrets.homepageDisplay is required"
    .Values.quotesSecrets.homepageDisplay | b64enc | quote }}
type: Opaque
