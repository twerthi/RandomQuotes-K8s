apiVersion: v1
kind: Service
metadata:
  name: {{ include "randomquotes.fullname" . }}-cluster-ip
  labels:
  {{- include "randomquotes.labels" . | nindent 4 }}
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
spec:
  type: {{ .Values.quotesAppClusterIpService.type }}
  selector:
    component: randomquotes-web
    {{- include "randomquotes.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.quotesAppClusterIpService.ports | toYaml | nindent 2 }}
