apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openshift-gitops-operator-randomquotes-petlife-production
  namespace: openshift-gitops
  annotations:
    argo.octopus.com/project: "openshift-gitops-operator-demo"
    argo.octopus.com/environment: "production" 
    argo.octopus.com/tenant: "pet-life"
    argo.octopus.com/image-replace-paths: "{{ .Values.quotesDeployment.randomquotesWeb.image.repository }}:{{ .Values.quotesDeployment.randomquotesWeb.image.tag }}" 
spec:
  project: default
  source:
    repoURL: https://github.com/twerthi/RandomQuotes-K8s.git
    targetRevision: HEAD
    path: helm
    helm:
      releaseName: randomquotes-petlife-production
      valueFiles:
        - tenants/petlife/prod/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: randomquotes-petlife-production
  syncPolicy:
#    automated: 
#      prune: true
#      selfHeal: true
    syncOptions:
    - Validate=false
    - CreateNamespace=true